services:
  api:
    build: .
    ports:
      - "9000:8000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_MODEL=${OPENAI_MODEL:-gpt-4o}
      - REDIS_URL=redis://redis:6379/0
      - CACHE_TTL_HOURS=${CACHE_TTL_HOURS:-168}
      - HTTP_TIMEOUT=${HTTP_TIMEOUT:-30}
      - HTTP_RETRIES=${HTTP_RETRIES:-3}
      - HTTP_JS_WAIT=${HTTP_JS_WAIT:-5000}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - app-network

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  redis_data:
